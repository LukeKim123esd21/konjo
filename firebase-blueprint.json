{
  "entities": {
    "User": {
      "title": "User",
      "description": "User profile information",
      "type": "object",
      "properties": {
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "role": { "type": "string", "enum": ["NORMAL", "VIP"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["email", "displayName", "role", "createdAt", "updatedAt"]
    },
    "PortfolioItem": {
      "title": "PortfolioItem",
      "description": "Instagram-style portfolio image",
      "type": "object",
      "properties": {
        "image": { "type": "string" },
        "title": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["image", "title", "createdAt"]
    },
    "SiteContent": {
      "title": "SiteContent",
      "description": "Dynamic text content for the landing page",
      "type": "object",
      "properties": {
        "heroTitle": { "type": "string" },
        "heroDesc": { "type": "string" },
        "footerText": { "type": "string" }
      }
    },
    "Schedule": {
      "title": "Schedule",
      "description": "Public calendar events",
      "type": "object",
      "properties": {
        "date": { "type": "string", "description": "YYYY-MM-DD" },
        "title": { "type": "string" },
        "type": { "type": "string", "enum": ["BOOKED", "AVAILABLE", "EVENT"] },
        "createdBy": { "type": "string" }
      },
      "required": ["date", "title", "type"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "User profiles"
    },
    "/portfolio/{itemId}": {
      "schema": "PortfolioItem",
      "description": "Portfolio archive"
    },
    "/settings/content": {
      "schema": "SiteContent",
      "description": "Global CMS settings"
    },
    "/schedules/{scheduleId}": {
      "schema": "Schedule",
      "description": "Public booking schedules"
    }
  }
}
